<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.botarmy.tourwala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tourwala"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tourwala"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tourwala">
<!--        <activity android:name=".UserProfile"></activity>-->
        <activity android:name=".MainActivity">
        </activity>
        <activity
            android:name=".home"
            android:theme="@style/AppTheme.NoActionBar">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".register_form">

        </activity>
        <activity android:name=".login_form">

        </activity>
    </application>

</manifest>